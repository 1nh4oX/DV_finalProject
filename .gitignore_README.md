# .gitignore 配置说明

## 📋 当前配置

### ✅ 允许提交的文件
- **数据文件**: `climate/data/raw/` 和 `traffic/data/sample/` 中的所有数据文件
- **代码文件**: 所有 `.py` 代码文件
- **文档文件**: 所有 `.md` 文档文件

### 🔒 已忽略的文件（保护隐私）

#### API Token 和密钥
- `.kaggle/` - Kaggle API 配置目录
- `kaggle.json` - Kaggle API Token 文件
- `*.key`, `*.token`, `*.secret` - 所有密钥文件
- `.env`, `.env.local` - 环境变量文件
- `**/kaggle.json` - 任何位置的 kaggle.json

#### 输出文件（防止仓库过大）
- `climate/output/` - 气候模块输出
- `traffic/output/` - 交通模块输出
- `*.png`, `*.jpg`, `*.pdf` - 生成的图表文件

---

## ⚠️ 重要提醒

### 不要提交以下内容：

1. **API Token**
   - Kaggle API Token (`~/.kaggle/kaggle.json`)
   - 任何包含 token 的文件
   - 环境变量文件 (`.env`)

2. **大文件**
   - 生成的图表文件（已自动忽略）
   - 输出文件夹（已自动忽略）

3. **个人配置**
   - IDE 配置文件
   - 系统文件 (`.DS_Store`)

---

## 🔧 如何配置 API Token（不提交到 Git）

### 方法1：使用环境变量（推荐）
```bash
export KAGGLE_API_TOKEN=your_token_here
```

### 方法2：使用本地配置文件
```bash
# 配置文件在 ~/.kaggle/kaggle.json（已在 .gitignore 中）
# 不会提交到 Git
```

### 方法3：使用配置脚本
```bash
python climate/code/setup_kaggle.py
```

---

## 📝 检查是否误提交了敏感信息

如果担心误提交了 API token，可以检查：

```bash
# 检查 Git 历史中是否包含敏感信息
git log --all --full-history --source -- "*kaggle.json"
git log --all --full-history --source -- "*token*"
```

如果发现误提交，需要：
1. 从 Git 历史中删除敏感文件
2. 更新 API Token（如果已泄露）
3. 确保 `.gitignore` 正确配置

---

## ✅ 验证配置

运行以下命令验证数据文件可以被提交：

```bash
# 检查数据文件是否被忽略
git check-ignore climate/data/raw/*.csv

# 如果没有输出，说明文件可以被提交
# 如果有输出，说明文件被忽略了
```

---

## 📚 相关文档

- Kaggle API 配置: `climate/README.md`
- 数据下载指南: `climate/code/download_data.py`

